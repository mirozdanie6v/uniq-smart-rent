(()=>{const params=new URLSearchParams(location.search);if(!params.has('layoutAudit'))return;const wait=ms=>new Promise(resolve=>setTimeout(resolve,ms));const finish=failures=>{const node=document.createElement('pre');node.id='api-audit-result';node.style.display='none';node.textContent=failures.length?`API_AUDIT_FAIL::${encodeURIComponent(failures.join('|'))}`:'API_AUDIT_OK';document.body.append(node);document.body.dataset.apiAudit=failures.length?'fail':'ok';document.body.dataset.apiAuditDetails=failures.join('|')};window.addEventListener('load',async()=>{const failures=[];try{const healthResponse=await fetch('/api/health',{cache:'no-store'});const health=await healthResponse.json();if(!healthResponse.ok||health.service!=='auto-sale-usa'||health.d1!==true||Number(health.schemaVersion)!==7||health.demoMode!==true||Number(health.demoBusinessOrders)!==2)failures.push('api-health')}catch{failures.push('api-health-unavailable')}try{await wait(350);const stateResponse=await fetch('/api/auto-sale/state',{cache:'no-store'});const state=await stateResponse.json();if(!stateResponse.ok||state.initialized!==true||Number(state.revision)<1||!Array.isArray(state.leads)||state.leads.length<1||!Array.isArray(state.quotes)||state.quotes.length<1||!Array.isArray(state.orders)||state.orders.length<1)failures.push('d1-state');if(stateResponse.ok&&state.demoBusiness!==true)failures.push('demo-business-missing');const demoOrders=Array.isArray(state.orders)?state.orders.filter(x=>String(x?.id||'').startsWith('O-DEMO-')):[];if(!demoOrders.some(x=>x.stage==='Доставка'))failures.push('demo-delivery-missing');if(!demoOrders.some(x=>x.stage==='Выдача'))failures.push('demo-handoff-missing')}catch{failures.push('d1-state-unavailable')}finish(failures)})})();